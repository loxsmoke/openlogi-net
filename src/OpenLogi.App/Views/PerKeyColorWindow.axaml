<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="using:OpenLogi.App.Localization"
        xmlns:vm="using:OpenLogi.App.ViewModels"
        xmlns:ctrl="using:OpenLogi.App.Controls"
        x:Class="OpenLogi.App.Views.PerKeyColorWindow"
        x:DataType="vm:PerKeyColorViewModel"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Icon="/Assets/keyboard.png"
        Title="{loc:Tr PerKeyColor_ColorKeys}">
    <StackPanel Margin="16" Spacing="12">
        <TextBlock TextWrapping="Wrap"
                   Text="{loc:Tr PerKeyColor_PressPhysicalKeyKeyboardPaint}"/>
        <TextBlock Text="{loc:Tr PerKeyColor_PaintColor}" FontWeight="SemiBold"/>
        <ctrl:QuickColorPicker Color="{Binding SelectedColor}" ShowOff="True" HorizontalAlignment="Left"/>
        <TextBlock Text="{loc:Tr PerKeyColor_BaseColor}" FontWeight="SemiBold"/>
        <ctrl:QuickColorPicker Color="{Binding BaseColor}" ShowOff="True" HorizontalAlignment="Left"/>
        <Button Content="{loc:Tr PerKeyColor_ResetAllKeysBaseAlt}" Click="OnResetAll"
                Focusable="False" IsTabStop="False"/>
        <TextBlock Text="{loc:Tr PerKeyColor_SpecialKeysOSSwallowsThese}" FontWeight="SemiBold"/>
        <ItemsControl ItemsSource="{Binding SpecialKeyRows}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><StackPanel Spacing="2"/></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ItemsControl ItemsSource="{Binding}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:SpecialKey">
                                <Button Content="{Binding Label}" Width="64" FontSize="11" Margin="0,0,6,0"
                                        HorizontalContentAlignment="Center"
                                        Command="{Binding $parent[Window].((vm:PerKeyColorViewModel)DataContext).PaintSpecialCommand}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Border Background="#15000000" Padding="10" CornerRadius="6">
            <TextBlock Text="{Binding Status}" TextWrapping="Wrap"/>
        </Border>
    </StackPanel>
</Window>
